/** Bootstrap contextual variants cycled across sections for the legend, chart and swatches. */
export const SECTION_VARIANTS = ['primary', 'warning', 'success', 'dark', 'info'] as const;

export const WEIGHTAGE_CELL_WIDTH = 140;
export const ACTIONS_CELL_WIDTH = 150;
